name: Full Flutter CI

on:
  # Manual only in Uzaktan Kontrol; builds run from "Flutter Nightly Build".
  workflow_dispatch:

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
